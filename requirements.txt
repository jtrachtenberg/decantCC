# Decant eval harness.
#
# The harness core and its whole test suite run with NOTHING installed
# (stdlib only, FakeModelClient) — install these only to run against real
# models and real corpora:

# Real model client (Opus/Haiku target tiers + the judge).
anthropic>=0.40

# Optional: author question sets in YAML instead of JSON.
# pyyaml>=6.0
